#include "main.h"
/**
 * _strlen - function prints lenghts of a string
 *
 * @s: variable to iterate o get string
 * Return: returns length
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{}
	return (i);
}
